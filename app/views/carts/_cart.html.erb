<h2><%= t('.title') %></h2>
<table class="table table-striped table-hover cart_table">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Qty</th>
      <!-- <th>Unit Price</th> -->
      <th>Price</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <%= render cart.line_items %>
    <tr>
      <td></td>
      <td>Total Price</td>
      <!-- <td></td> -->
      <td></td>
      <td><%= number_to_currency(cart.total_price) %></td>
      <td></td>
    </tr>
  </tbody>
</table>

<%= button_to t('.checkout'), new_order_path, method: :get, :class => 'btn btn-success' %>
<%= button_to t('.empty'), cart, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger' %>
